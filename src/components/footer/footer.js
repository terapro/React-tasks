import React from 'react';

import {Logo} from 'src/components/common/logo/logo.js';

const Footer = () => (
        <footer className={'footer'}>
            <Logo />
        </footer>
    );
export {Footer};